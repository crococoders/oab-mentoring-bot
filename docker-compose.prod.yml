version: '3'
services:

  postgres:
    volumes:
      - "postgres-data:/data/postgres"
    env_file:
      - docker-compose.prod.postgres.env

  bot:
    env_file:
      - docker-compose.prod.bot.env
      
  nginx:
    restart: always
    build:
        context: ./nginx
    ports:
        - "80:80"
    depends_on:
      - bot

volumes:
  postgres-data: